<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/loginFragment">

 <fragment
     android:id="@+id/loginFragment"
  tools:layout="@layout/fragment_login"
  android:label="LoginFragment">
     <action
         android:id="@+id/action_loginFragment_to_pickCategoriesActivity"
         app:destination="@id/pickCategoriesActivity" />
 </fragment>
    <activity
        android:id="@+id/pickCategoriesActivity"
        android:name="com.example.primaapplicazioneandroid.PickCategoriesActivity"
        android:label="activity_pick_categories"
        tools:layout="@layout/activity_pick_categories" />

</navigation>